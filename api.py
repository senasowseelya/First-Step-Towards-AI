'''
1.For a python application to talk to an external api, we have a package called request
2.pip install requests
3.import requests
4.requests.get(url) --> returns a response
5.response returned is a string and we can convert it to json if it is json response.
'''